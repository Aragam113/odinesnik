/* Последний проход: в части заданий дистрактор должен быть длиннее верного,
   иначе «выбери самый длинный» продолжает работать даже при близких средних. */
const fs = require('fs'), path = require('path');
const P = [
['Без разницы: платформа сама перенесёт условие куда нужно','Без разницы: платформа сама перенесёт условие в нужное место запроса'],
['Левое соединение в 1С не поддерживает виртуальные таблицы регистров','Левое соединение в 1С не поддерживает виртуальные таблицы регистров накопления'],
['СрезПоследних возвращает все записи, отсортированные по дате','СрезПоследних возвращает все записи регистра, отсортированные по дате'],
['Да, но выполняться будет заметно медленнее обычного','Да, но выполняться будет заметно медленнее, чем на сервере'],
['Когда процедура вызывается из модуля объекта','Когда процедура вызывается из модуля объекта, а не из формы'],
['Реквизит Цена прямо в карточке справочника Номенклатура','Реквизит Цена прямо в карточке справочника Номенклатура, без истории'],
['Измерение хранит числа, ресурс — ссылки, реквизит — строки','Измерение хранит числа, ресурс хранит ссылки, а реквизит только строки'],
['Журнал регистрации на предмет входа пользователя','Журнал регистрации: кто и когда заходил в базу перед этим'],
['Включить автоматические блокировки в свойствах конфигурации','Включить автоматические блокировки в свойствах всей конфигурации'],
['Полная блокировка базы при обновлении конфигурации','Полная блокировка базы на время обновления конфигурации'],
['Это одно и то же, разные названия в разных версиях','Это одно и то же: разные названия в разных версиях платформы'],
['Доработки сохранятся автоматически, платформа их объединит','Доработки сохранятся автоматически: платформа сама их объединит'],
['Не настроено условное оформление строк','Не настроено условное оформление строк в настройках отчёта'],
['В табличный документ нельзя вывести данные программно','В табличный документ нельзя вывести данные программно из кода'],
['Вендор прекратил выпуск обновлений для этого продукта','Вендор прекратил выпуск обновлений для этого прикладного решения']
];
const file = path.join(__dirname, '..', 'src', 'data', 'exercises.js');
let s = fs.readFileSync(file, 'utf8'), done = 0, missed = [];
for (const [from, to] of P) {
  const q = '"' + from + '"';
  if (s.indexOf(q) < 0) { missed.push(from.slice(0, 46)); continue; }
  s = s.split(q).join('"' + to + '"'); done++;
}
fs.writeFileSync(file, s, 'utf8');
console.log('заменено:', done, 'из', P.length);
missed.forEach(m => console.log('  не найдено: ' + m));
