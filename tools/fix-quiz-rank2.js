const fs = require('fs'), path = require('path');
const P = [
['ОБЪЕДИНИТЬ соединяет таблицы по ключу, ОБЪЕДИНИТЬ ВСЕ — по всем полям','ОБЪЕДИНИТЬ соединяет таблицы по ключу, а ОБЪЕДИНИТЬ ВСЕ — сразу по всем полям'],
['Да, но выполняться будет заметно медленнее, чем на сервере','Да, но выполняться этот код будет заметно медленнее, чем на сервере'],
['Реквизит Цена прямо в карточке справочника Номенклатура, без истории','Реквизит Цена прямо в карточке справочника Номенклатура, но без истории'],
['Измерение хранит числа, ресурс хранит ссылки, а реквизит только строки','Измерение хранит числа, ресурс хранит ссылки, а реквизит только текстовые строки'],
['Журнал регистрации: кто и когда заходил в базу перед этим','Журнал регистрации: кто и когда заходил в базу незадолго до этого'],
['Включить автоматические блокировки в свойствах всей конфигурации','Включить автоматические блокировки данных в свойствах всей конфигурации'],
['Полная блокировка базы на время обновления конфигурации','Полная блокировка базы данных на время обновления конфигурации'],
['Это одно и то же: разные названия в разных версиях платформы','Это одно и то же: просто разные названия в разных версиях платформы'],
['Не настроено условное оформление строк в настройках отчёта','Не настроено условное оформление строк в пользовательских настройках'],
['В табличный документ нельзя вывести данные программно из кода','В табличный документ нельзя вывести данные программно прямо из кода']
];
const file = path.join(__dirname, '..', 'src', 'data', 'exercises.js');
let s = fs.readFileSync(file, 'utf8'), done = 0;
for (const [from, to] of P) { const q = '"' + from + '"'; if (s.indexOf(q) >= 0) { s = s.split(q).join('"' + to + '"'); done++; } }
fs.writeFileSync(file, s, 'utf8');
console.log('заменено:', done, 'из', P.length);
